import React from 'react'

const Effects = () => {
  return (
    <div>
      <button id="brightness" className="active">Brilho</button>
      <button id="saturation">Saturação</button>
      <button id="inversion">Negativo</button>
      <button id="grayscale">Black & White</button>
    </div>
  );
}

export default Effects